# URL do backend em produção (Render).
# Carregado automaticamente pelo Vite em `npm run build`.
# Uma variável VITE_API_URL definida no ambiente de build (painel da Vercel,
# onde este frontend é publicado) tem precedência sobre este arquivo.
VITE_API_URL=https://mem-words-backend.onrender.com
